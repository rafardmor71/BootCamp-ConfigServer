server:
  port: 8888
 
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rafardmor71/BootCamp-ConfigServer
          default-label: main
          search-paths: 
            - '*propiedades'
        